<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.led.parcapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowSingleVehiculeRecordActivity"></activity>
        <activity android:name=".ShowAllVehiculeActivity" />
        <activity android:name=".AjouterMission" />
        <activity android:name=".AjouterVehicule" />
        <activity android:name=".Register" />
        <activity android:name=".AjouterChauffeur" />
        <activity android:name=".gestionparc" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>